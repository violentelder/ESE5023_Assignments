ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq <- c(1,2,3,4,5)
LargestEq <- c()
for (i in length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
LargestEq
CountEq
CountEq <- c(1,2,3,4,5)
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[1])[1]))
CountEq
CountEq <- c(1,2,3,4,5)
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[1])[2]))
CountEq
CountEq <- c(1,2,3,4,5)
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq('CHINA')[1]))
CountEq
CountEq_LargestEq(Country[1])[1]
CountEq_LargestEq(as.character(Country[1]))[1]
Country[1]
typeof(Country[1])
typeof('CHINA')
temp <- Country[1]
CountEq_LargestEq(temp)[1]
CountEq_LargestEq('CHINA')[2]
CountEq_LargestEq("CHINA")[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
temp <- Country[1]
CountEq_LargestEq(as.character(temp))[1]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
temp <- Country[9]
CountEq_LargestEq(as.character(temp))[1]
temp <-
CountEq_LargestEq(as.character(Country[9]))[1]
CountEq_LargestEq(as.character(Country[9]))[1]
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
n()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.Date(as.character(LargestEq))
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.Date(as.character(LargestEq))
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(as.character(Country[9]))[1]
CountEq_LargestEq(as.character(Country[9]))[2]
CountEq_LargestEq(Country[9])[2]
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(is.null(LargestEq))
ans <- list(CountEq,LargestEq)
else{
LargestEq <- as.Date(as.character(LargestEq))
ans <- list(CountEq,LargestEq)
}
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(is.null(LargestEq))
ans <- list(CountEq,LargestEq)
else{
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
}
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq <- c()
LargestEq <- c()
for (i in length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
LargestEq
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,CountEq_LargestEq(Country[i])[2])
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,CountEq_LargestEq(Country[i])[2])
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(LargestEq = 0)
LargestEq <- "Loss Data"
else
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[1])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(LargestEq = "character(0)")
LargestEq <- "Loss Data"
else
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
CountEq_LargestEq('CHINA')[2]
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(is.na(LargestEq))
LargestEq <- "Loss Data"
else
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
mutate(new_EQ_PRIMARY = ifelse(is.na(EQ_PRIMARY),0,EQ_PRIMARY)) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(is.na(LargestEq))
LargestEq <- "Loss Data"
else
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
if(is.na(LargestEq))
LargestEq <- "Loss Data"
else
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
LargestEq <- "Loss Data"
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- "Loss Data"
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- "Loss Data"
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- "Loss Data"
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
#1.4
CountEq_LargestEq <- function(country,tib = Sig_Eqs){
CountEq <- tib %>%
filter(COUNTRY == country) %>%
nrow()
LargestEq <- "Loss Data"
LargestEq <- tib %>%
filter(COUNTRY == country) %>%
filter(EQ_PRIMARY == max(EQ_PRIMARY,na.rm = T)) %>%
mutate(DATE = paste(YEAR,MONTH,DAY,sep = "-")) %>%
select(DATE)
LargestEq <- as.character(LargestEq)
ans <- list(CountEq,LargestEq)
return(ans)
}
CountEq_LargestEq('CHINA')[2]
Country <- Sig_Eqs %>%
pull(COUNTRY)
Country<- Country[!duplicated(Country)]
CountEq_LargestEq(Country[50])[2]
CountEq <- c()
LargestEq <- c()
for (i in 1:length(Country)) {
CountEq <- c(CountEq,as.numeric(CountEq_LargestEq(Country[i])[1]))
if(CountEq_LargestEq(Country[i])[2] != 'character(0)')
LargestEq <- c(LargestEq,as.character(CountEq_LargestEq(Country[i])[2]))
else
LargestEq <- c(LargestEq,"Loss Data")
}
new_eqs <- data.frame(Country,CountEq,LargestEq)
new_eqs
